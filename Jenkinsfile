#!groovy

pipeline {
	agent any

	stages {
		stage('Build') {
			steps {
				sh 'pwd'
			}
		}

		stage('Test') {
			steps {
				sh 'pwd'
			}
		}

		stage {
			steps {
				sh 'java -version'
			}
		}
	}
}

